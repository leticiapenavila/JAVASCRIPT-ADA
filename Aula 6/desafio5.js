let valores = {
    valor1: 10,
    valor2: 20,
    valor3: 30,
    valor4: 40,
  };
  
  for (let propriedade in valores){
    console.log(propriedade = valores.valor1 + valores.valor2 + valores.valor3 + valores.valor4)
  }